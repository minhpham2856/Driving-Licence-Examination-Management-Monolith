ld` / `setField` (or `isField` for booleans). No parsing, validation, delegation to nested models, or computed display logic inside DTO classes.

**Exception:** [`ServiceResult`](src/java/examiner/dto/ServiceResult.java) stays a result wrapper as a **Java record** with static `ok`/`fail` factories and `isSuccess()` (per your spec).

---

## Current problems (by DTO)

| DTO | Issues |
|-----|--------|
| [`CandidateRowDTO`](src/java/examiner/dto/CandidateRowDTO.java) | `getSbd()` alias; computed `getStatus()`, `getStatusLabel()`, `getSexValue()`, `getSexLabel()`, `isAwaitingSignature()` |
| [`EnrollmentDTO`](src/java/examiner/dto/EnrollmentDTO.java) | Wraps `Candidate` + `ExamEnrollment`; 20+ derived getters (`getId`, `getExamId`, `getFullName`, `parseCandidateNumber`, `getSectionStatusEnum`, setters mutating nested models) |
| [`ExportPayloadDTO`](src/java/examiner/dto/ExportPayloadDTO.java) | `primaryHeaders()`, `primaryRows()`, `toXmlDocument()` helpers |
| [`ExportContextDTO`](src/java/examiner/dto/ExportContextDTO.java) | `sectionTypeValue()` helper |
| [`ExamDispatchResult`](src/java/examiner/dto/ExamDispatchResult.java) | Field `promotedSbd` / `getPromotedSbd()` — rename to `promotedCandidateNumber` |
| [`ExamStatsDTO`](src/java/examiner/dto/ExamStatsDTO.java), [`SaveResultDTO`](src/java/examiner/dto/SaveResultDTO.java), [`PrintPreviewDTO`](src/java/examiner/dto/PrintPreviewDTO.java), [`XmlExportTable`](src/java/examiner/dto/XmlExportTable.java), [`XmlExportDocument`](src/java/examiner/dto/XmlExportDocument.java) | Already compliant (minor review only) |

---

## 1. Convert ServiceResult to record

Replace class with your record shape in [`ServiceResult.java`](src/java/examiner/dto/ServiceResult.java):

```java
public record ServiceResult<T>(boolean success, ErrorType errorType, String message, T data) {
    public static <T> ServiceResult<T> ok(T data) { ... }
    public static <T> ServiceResult<T> fail(ErrorType type, String message) { ... }
    public boolean isSuccess() { return success; }
}
```

No other API changes — all existing `ServiceResult.ok(...)` / `.fail(...)` call sites in examiner module keep working.

---

## 2. CandidateRowDTO — remove helpers, add display fields

### Remove from DTO
- `getSbd()` (alias)
- `getStatus()`, `getStatusLabel()`, `getSexValue()`, `getSexLabel()`, `isAwaitingSignature()`

### Add fields (populated at build time)
- `String status` — CSS key: `pending` / `testing` / `awaiting` / `done` / `suspended`
- `String statusLabel` — Vietnamese label for tags
- `String sexValue` — `"0"` / `"1"` for forms
- `String sexLabel` — display text
- `boolean awaitingSignature`

### Move logic to [`ExamViewServiceImpl.buildCandidateRow`](src/java/examiner/service/impl/ExamViewServiceImpl.java)

Extract the removed DTO logic into private helpers in the service (or [`examiner.util.FormatUtil`](src/java/examiner/util/FormatUtil.java)):

```java
// Example: called from buildCandidateRow after setSectionStatus / setSuspende