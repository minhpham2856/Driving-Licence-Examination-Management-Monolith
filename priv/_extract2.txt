resolveStatusKey(sectionStatus, suspended));\nrow.setStatusLabel(resolveStatusLabel(sectionStatus, suspended));\nrow.setSexValue(sex == Sex.FEMALE ? \"1\" : \"0\");\nrow.setSexLabel(resolveSexLabel(sex));\nrow.setAwaitingSignature(sectionStatus == CandidateStatus.AWAITING_SIGNATURE);\n```\n\n### JSP updates (EL property renames)\n\nHTTP query params stay `sbd` — only **DTO property access** changes:\n\n| Old EL | New EL |\n|--------|--------|\n| `${c.sbd}` | `${c.candidateNumber}` |\n| `${candidate.sbd}` | `${candidate.candidateNumber}` |\n| `${c.status}` | unchanged (now a field, not computed) |\n| `${c.statusLabel}` | unchanged |\n| `${c.awaitingSignature}` | unchanged |\n| `${candidate.sexLabel}` | unchanged |\n\nPrimary files: [`candidate-list.jsp`](web/views/examiner/components/candidate-list.jsp), [`candidate-details.jsp`](web/views/examiner/candidate-details.jsp), [`toolbar.jsp`](web/views/examiner/components/toolbar.jsp), [`score-entry.jsp`](web/views/examiner/score-entry.jsp), [`result-details-edit.jsp`](web/views/examiner/result-details-edit.jsp), [`faults.jsp`](web/views/examiner/components/faults.jsp), [`document-rows.jsp`](web/views/examiner/components/document-rows.jsp), [`export-row.jsp`](web/views/examiner/components/export-row.jsp), [`candidate-paper.jsp`](web/views/examiner/candidate-paper.jsp).\n\n---\n\n## 3. EnrollmentDTO — flatten to explicit fields\n\nReplace the composite `Candidate` + `ExamEnrollment` wrapper with **flat scalar fields** populated in [`EnrollmentServiceImpl.toEnrollmentDto`](src/java/examiner/service/impl/EnrollmentServiceImpl.java).\n\n### Proposed fields (get/set match field names)\n\n**Identity / exam**\n- `candidateId`, `examEnrollmentId`, `examId`, `candidateNumber` (int, parsed once in service via existing `FormatUtil` / `parseCandidateNumber` logic moved to service)\n\n**Profile**\n- `fullName`, `dateOfBirth` (`Timestamp`), `governmentIdNumber`, `phoneNumber`, `address`, `email`, `reasonForTaking`, `sex` (boolean), `ph